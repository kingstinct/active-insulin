import SwiftUI

extension Color {
  static let AlmostBlack = Color.init(red: 0.15, green: 0.15, blue: 0.15);
  static let AlmosterBlack = Color.init(red: 0.05, green: 0.05, blue: 0.05);
  static let AccentColor = Color(UIColor.magenta) // Color("AccentColor");
}

